name: 'streampulse_dbt'
version: '1.0.0'

profile: 'streampulse_dbt'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  streampulse_dbt:
    staging:
      +materialized: view
    silver:
      +materialized: table
